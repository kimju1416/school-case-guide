# 사실 검수 규칙 (정확성이 생명)

대상: `research/<id>.json` (교사용 공개 사이트 «학교 사안 처리 길잡이»의 내용). 오늘 2026-09-24.
형식은 `_SCHEMA.md` 참고. **다른 검수원도 동시에 다른 파일을 고치고 있으니, 맡은 파일만 고친다.**

## 무엇을 검수하나
파일 안의 **모든 사실 주장**. 특히:
1. 숫자: 기한·일수·시간·기간(기산점 포함 — «안 날부터 / 있은 날부터 / 통지받은 날부터»), 금액(과태료·벌금 상한), 인원·횟수, 호수(1~9호 등)
2. 법령: 법률 이름·조·항·호 번호가 그 내용과 실제로 맞는지, **현재 시행 중인 조문**인지(개정·신설·삭제 확인)
3. 전화번호·기관 이름(최근 이름이 바뀐 기관 주의)
4. 법적 효과: «~해야 한다(의무)» vs «~할 수 있다(재량)», 처벌 여부, 면책 조건, 청구권자, 피청구인/피고
5. 절차 순서와 주체(누가 하는가 — 학교장/교육장/교육감/위원회)
6. 출처: 제목·발행기관·연도가 실제와 맞는지, 2021년 이후인지
7. deep·tables·faq·dont·first 안의 서술도 전부 포함

## 어떻게 확인하나
- 법령: 국가법령정보센터 현행 조문(law.go.kr). WebFetch가 안 되면 `curl --ssl-no-revoke -L`로 받아 읽는다. 법령 본문 API 예: `https://www.law.go.kr/DRF/lawService.do?OC=test&target=law&type=XML&MST=...` 대신 `https://www.law.go.kr/법령/<법령명>` 페이지나 `/LSW/lsInfoP.do` 등 가능한 방법을 쓴다. (research/src/ 에 이미 받은 법령 텍스트·매뉴얼 PDF 텍스트가 많다 — 먼저 거기서 grep)
- 매뉴얼: research/src/ 의 텍스트, 없으면 공식 게시처에서 받아 pdfplumber로 추출.
- **기억이나 짐작으로 «맞다»고 판정하지 않는다.** 원문 문장을 찾은 것만 «확인».

## 판정과 처리
- 확인됨 → 그대로 둔다.
- **틀림** → 원문대로 고친다(JSON 직접 수정). 근거 칸(basis)도 맞게 고친다.
- **확인 불가** → 본문에서 단정 표현을 빼거나 완화하고, 그 내용을 `uncertain`에 «…은 원문 확인 필요»로 옮긴다. 확인 안 된 숫자를 본문에 남기지 않는다.
- 새 내용을 추가하지 않는다(검수이지 보강이 아니다). 단, 틀린 것을 바로잡는 데 필요한 최소한은 괜찮다.
- 공개 사이트라 보호자·학생 응대 대본(script)은 넣지 않는다.
- 고친 뒤 JSON이 제대로 읽히는지 `node -e "JSON.parse(require('fs').readFileSync(f))"`로 확인.

## 기록
파일마다 `research/verify/<회차>-<id>.md`:
```
# <회차> <id> 검수 — 확인 N · 고침 N · 확인 불가 N
| 칸 | 원래 내용 | 판정 | 원문 근거(법령 조항 / 매뉴얼 쪽) | 고친 내용 |
```
«확인됨»은 한 줄씩 다 쓰지 않아도 되고 개수만 적어도 된다. «고침»과 «확인 불가»는 빠짐없이.
끝나면 짧게 보고: 파일별 확인/고침/불가 개수와, 고친 것 중 중요한 것(법적 효과·기한·번호가 바뀐 것).

## 🔴 전국판 규칙 (2026-09-24 형님 지시 — 2차부터 적용)
이 사이트는 **전국 교사용**이다. 경상북도(경북) 전용 정보는 모두 뺀다.
- 경북 전용 번호·기관(경북교육청·경북학교안전공제회·경북 Wee센터·054-… 번호·경북 디지털성범죄 센터 등)은 삭제하거나
  **전국 공통 표현**으로 바꾼다: «소속 시·도교육청», «해당 시·도 학교안전공제회», «지역번호+1395(시·도 교육활동보호센터)», «관할 교육지원청 Wee센터» 등.
- 경북 기준 서술(«경북은 …», «경북 조례»)은 빼거나 «시·도마다 다름 — 소속 교육청 기준 확인»으로 바꾼다.
- 특정 시·도 매뉴얼(충북·인천·경기·서울 등)을 근거로 쓴 절차는 «○○교육청 예시»임을 밝히거나, 전국 공통(법령·교육부 자료)으로 확인되는 것만 단정한다.
- 시·도 매뉴얼 출처는 남겨도 되지만, 본문이 그 시·도만의 규칙을 전국 규칙처럼 말하면 안 된다.

## 🔵 전수 재검(2026-09-24 밤) — 코덱스 검수판 위에서 다시
- 기준: 코덱스 전수 검수(8c7c0ac·8efe319) 결과를 합친 현재 파일. 회차 이름은 «전수». 기록은 research/verify/전수-<id>.md.
- **코덱스가 바꾼 곳을 특히 본다**: `git diff 422cd09 HEAD -- research/<id>.json` 로 코덱스가 고치거나 새로 넣은 문장을 뽑아, 하나하나 원문과 대조한다(새로 넣은 절차·기한·조문은 특히 엄격히).
- 그 밖의 모든 사실 주장도 처음부터 다시 본다(앞 검수를 믿지 않는다).
- 경북 전용 흔적(경북·경상북도·054-)은 전국판 규칙대로 정리.

## 🟣 3회 검사(2026-09-25) — 전수 재검(ba45a2a) 위에서 한 번 더
- 회차 이름 «3회». 기록은 research/verify/3회-<id>.md.
- **먼저**: `git diff 555b13b ba45a2a -- research/<id>.json` 으로 지난 «전수» 재검이 바꾼 문장을 모두 뽑아, 그 고침이 원문과 맞는지 하나씩 확인한다(고치면서 새로 생긴 실수 찾기). research/verify/전수-<id>.md 의 근거 쪽수·조문도 직접 열어 확인.
- **그다음**: 파일 전체의 모든 사실 주장을 처음부터 다시 원문과 대조(앞 검수를 믿지 않는다). 특히 «해야 한다/할 수 있다», «금지/예외», 기한의 기산점, 주체(학교장·교육장·교육감·위원회), 시행일.
- 원문은 research/src/ 와 C:/Users/USER/Documents/Codex/2026-09-24/https-kimju1416-github-io-school-case/work/ 를 먼저, 없으면 law.go.kr 등 공식 원문.
- 보고: 파일별 확인/고침/불가 개수 + «전수 재검 고침 중 틀린 것» + 중요한 고침.

## 🟤 4회 검사(2026-09-25) — 3회 검사판(6988c88) 위에서
- 회차 «4회». 기록은 research/verify/4회-<id>.md.
- **먼저**: `git diff ba45a2a 6988c88 -- research/<id>.json` 으로 3회가 바꾼 문장을 뽑아 원문과 하나씩 확인(3회 기록 research/verify/3회-<id>.md의 근거도 직접 열어 확인).
- **그다음**: 파일 전체를 처음부터 다시 원문과 대조(앞 검수를 믿지 않는다). 특히 의무/재량, 금지/예외, 기산점, 주체, 시행일, 숫자, 전화번호.
- **추가로 볼 것**: 한 파일 안에서 서로 어긋나는 서술(요약·첫 할 일·단계·표·FAQ가 같은 사실을 다르게 말하는 곳), 그리고 교사가 그대로 따랐을 때 위험해질 수 있는 단정 표현.
- 원문은 research/src/ 와 C:/Users/USER/Documents/Codex/2026-09-24/https-kimju1416-github-io-school-case/work/ 를 먼저, 없으면 law.go.kr 등 공식 원문.
- 보고: 파일별 확인/고침/불가 개수 + «3회 고침 중 틀린 것» + 중요한 고침.

## ⚫ 5회 검사(2026-09-25) — 오류 유형별 전 파일 가로 검사
지금까지 반복해서 나온 오류는 네 유형이다. 이번엔 **맡은 유형 하나를 21개 파일 전체에서** 뽑아 한 줄씩 원문과 대조한다.
1. 의무/재량: «~해야 한다·~한다·반드시» vs «~할 수 있다·권고·노력» — 법조문·고시·매뉴얼 문언과 정확히 같은지
2. 날짜·기간·숫자: 시행일·기준일(«○○ 이후 사안»)·기한과 기산점·일수·금액·시간·나이
3. 주체와 대상: 누가(학교장·교감·교육장·교육감·위원회·보건교사·경찰·지자체) 무엇을, 누구에게/누구의 사건에 — 범위가 넓거나 좁게 쓰였는지
4. 출처 범위: 특정 시·도(경기·서울·충북·인천) 자료나 예시가 전국 규칙처럼 단정됐는지, 원문에 없는 절차를 단정했는지
- 방법: `node` 로 research/*.json 의 모든 문자열 칸을 훑어 해당 유형 문장을 뽑는다 → 각 문장의 근거(basis·sources·원문)를 열어 대조 → 틀리면 고친다.
- **여러 검수원이 같은 파일을 동시에 고친다.** 고칠 때는 파일을 읽은 직후 해당 문자열만 바꾸는 node 스크립트로(전체 덮어쓰기 금지 — 다른 사람 고침이 날아감). 바꿀 원문 문자열이 없으면 다시 읽고 재시도.
- 기록: research/verify/5회-<유형>.md (고친 곳은 파일·칸·전후·근거 모두). 보고: 뽑은 문장 수·확인·고침·불가.

## ⚪ 6회 마지막 점검(2026-09-25) — 실무 순서·내부 일관성
원문 대조는 5회까지로 수렴했다. 이번엔 **현장 책임교사·교감의 눈**으로 본다. 기록 research/verify/6회-<묶음>.md.
1. 흐름: steps를 S01부터 끝까지 실제 사안을 처리하듯 따라간다. (a) 순서가 뒤바뀐 곳(보고 전에 조치, 조사 전에 결정 등) (b) 빠진 핵심 단계 — 교육부 가이드북·매뉴얼의 **공식 사안처리 흐름도/절차 순서**(research/src 원문)와 대조 (c) stage 값이 단계 내용과 맞는지 (d) 막다른 길(불복·종결로 이어지지 않는 경로).
2. 내부 일관성: 같은 사실을 summary·first·steps·deadlines·tables·faq·dont·deep 가 서로 다르게 말하는 곳. 다른 파일과 겹치는 사실(117·1388·1395·112, 행정심판 90/180일, 행정소송 90일/1년, 48시간 보고 등)도 확인.
3. 현장 위험: 교사가 그대로 따르면 곤란해질 단정·빠진 단서(«즉시», «반드시», 예외 조건).
- 고칠 땐 반드시 원문 근거가 있어야 한다. 근거 없으면 고치지 말고 기록에 «제안»으로만 남긴다. 새 단계 추가는 원문에 명시된 핵심 절차가 빠진 경우만.
- 파일은 읽은 직후 해당 문자열만 바꾸는 node 스크립트로. 커밋·배포 금지.
